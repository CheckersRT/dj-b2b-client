export default function sortTable(table, header) {
let switching = true;
let shouldSwitch;
let rows;
let x;
let y;

while (switching) {
    switching = false;
    rows = table.rows;

    // for(i = 1; i < rows.length -1; i++) {
    //     shouldSwitch = false;
    //     x = rows[i].getElementsByTagName("TD")[0];
    //     y = rows[i + 1].getElementsByTagName("TD")[0];
        
    // }

}

}